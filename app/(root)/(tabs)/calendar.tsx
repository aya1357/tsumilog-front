import { View } from 'react-native'

import { Calendar } from '@/components/calendar'

export default function Index() {
  return (
    <View style={{ flex: 1 }}>
      <Calendar />
    </View>
  )
}
